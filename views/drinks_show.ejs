<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitPub</title>
</head>
<body>
    <h1>At foo bar</h1>
    <a href="/drinks">back</a>
    <h2><%=drink.name.split("")
        <!-- not DRY, in real life would save the function used in index to uppercase name and call it it both instances but I wanted to practice doing it a couple ways  -->
        .map((letter, i) => i? letter:letter.toUpperCase())
        .join("") %></h2>
        <h3>$<%=drink.price %></h3>
        <img src="<%=drink.image %>" alt="<%drink.name%>">
        



    </ul>